# Spring: CRUD Zoo
CRUD para um sistema de gerenciamento de zoológico.

Para executar, basta entrar na pasta do projeto e rodar o comando mvn spring-boot:run.

Em seguida, entre na url: http://localhost:8080/, e estará na tela inicial do programa.
